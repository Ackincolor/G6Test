<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
  <ul>
    liste :
    <% if (testValue.length) {
    %><ul>
      <% testValue.forEach((book) => {
      %>
        <li><%= book.id %> <%= book.nom %></li>
      <%
      }) %>
    </ul><%
    } %>
  </ul>
  </body>
</html>
